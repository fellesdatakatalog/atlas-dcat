---
apiVersion: v1
kind: Service
metadata:
  name: fdk-atlas-dcat-service
spec:
  selector:
    app: fdk-atlas-dcat-service
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort
